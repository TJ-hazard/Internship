<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Nigerian Internship Portal - Login</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
   <link rel="stylesheet" href="css/styles.css">
</head>
<body class="bg-green-50 text-gray-800 flex flex-col min-h-screen">

  <!-- Vision Section -->
  <header class="bg-green-700 text-white py-8 shadow-md">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h1 class="text-3xl font-bold">Empowering Nigerian Talent through Digital Innovation</h1>
      <p class="mt-2 text-lg text-green-100">Secure, inclusive, and tech-driven registration for national internship programs.</p>
    </div>
  </header>

  <!-- Form Section -->
  <form action="/verified" method="POST" class="space-y-6 w-full max-w-sm mx-auto">
    <div>
      <% if (errors && errors.email) { %>
    <div class="alert-box"><%= errors.email %></div>
  <% } %>

  <div id="otp-alert" style="display: none;"></div>

      <label for="email" class="block text-left text-green-800 font-semibold mb-1">Email Address</label>
      <input type="email" id="email" name="email" required placeholder="e.g. user@example.com"
             class="w-full px-4 py-3 border border-green-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500" value="<%= formData?.email || '' %>">
    </div>
  
    <div>
    <% if (errors && errors.otp) { %>
    <div class="alert-box"><%= errors.otp %></div>
  <% } %>
      <label for="otp" class="block text-left text-green-800 font-semibold mb-1">One-Time Password (OTP)</label>
      <div class="flex space-x-2">
        <input type="text" id="otp" name="otp" required placeholder="Enter OTP"
               class="w-full px-4 py-3 border border-green-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500" value="<%= formData?.otp || '' %>">
  
        <button type="button"
                onclick="requestOtp()"
                class="px-4 py-2 bg-green-100 text-green-700 border border-green-600 rounded-xl hover:bg-green-200 text-sm font-medium transition" id="request_otp">
          Request
        </button>
      </div>
    </div>
  
    <button type="submit"
            class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-xl transition">
      Verify & Login
    </button>
  </form>
  

  <!-- About & Contact Section -->
  <footer class="bg-white border-t border-green-200 py-10">
    <div class="max-w-4xl mx-auto px-4">
      <h3 class="text-xl font-bold text-green-700 mb-4">About This Portal</h3>
      <p class="text-gray-700 mb-6">
        This platform is built to simplify internship registrations for Nigerian students, supervisors, and ITF officials.
        It uses secure email and OTP verification to ensure access control and data privacy across all user roles.
      </p>

      <h4 class="text-lg font-semibold text-green-700 mb-2">Contact Us</h4>
      <p class="text-gray-700 mb-1">📧 Email: <a href="mailto:support@itfportal.ng" class="text-green-600 underline">support@itfportal.ng</a></p>
      <p class="text-gray-700">📞 Phone: <a href="tel:+2348012345678" class="text-green-600 underline">+234 801 234 5678</a></p>
    </div>

    <div class="text-center text-sm text-green-700 mt-8">
      &copy; 2025 Nigerian ITF Registration Portal. All Rights Reserved.
    </div>
  </footer>



<!-- Java Script -->
  <script src="js/script.js">
  </script>
</body>
</html>

 